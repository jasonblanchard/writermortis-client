<header class='cf'>
  <h1 class='pull-left'>WM</h1>
  <div class='pull-right'>
    {{#if session.isAuthenticated}}
      {{currentUser.email}}
      <a href='' {{ action 'invalidateSession' }}>Logout</a>
    {{else}}
      {{#link-to 'login'}}Login{{/link-to}}    
    {{/if}}
  </div>
</header>

<main class='row'>
  <div class='col-md-12'>
    {{outlet}}
  </div>
</main>
