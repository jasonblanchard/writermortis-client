<h2>{{title}}</h2>
<div>
  Pieces:
  <br />
  {{#each pieces}}
  <span>{{text}} ({{this.user.email}})</span>
  {{/each}}
</div>

{{#if canPost}}
  <div>
    {{textarea value=newPiece.text}}
    <br />
    <button {{action 'createPiece'}}>Create Piece</button>
  </div>
{{else}}
  <strong>You added the last piece!</strong>
{{/if}}
  
